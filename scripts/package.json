{
  "name": "job-search-email-sync",
  "version": "1.0.0",
  "description": "Reads Apple Mail, classifies job emails with Claude, syncs to Google Sheets",
  "main": "sync-emails.js",
  "scripts": {
    "sync": "node sync-emails.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
